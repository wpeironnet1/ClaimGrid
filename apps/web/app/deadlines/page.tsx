import DeadlineTracker from "./DeadlineTracker";
import "./deadlines.css";

export const metadata = { title: "Deadline tracker — ClaimGrid", description: "Track source-backed county and federal mineral claim deadlines." };
export default function DeadlinesPage(){ return <DeadlineTracker/>; }
